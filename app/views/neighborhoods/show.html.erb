<section>
<%= render "banner" %>
</section>

<section class="neighborhood-details mb-5">
  <span class="d-flex align-items-baseline">
    <h1 class="neighborhood-name"><%= @neighborhood.name %></h1>
    <h2><%= @neighborhood.city.name %></h2>
  </span>
  <p class="neighborhood-description">
    <%= @neighborhood.description %>
  </p>
</section>

<section class="mb-5">
  <h3> O que gostaria de fazer </h3>
  <%= render 'shared/cards/card_category', elements: Location.categories %>
</section>

<section id="neighborhood-neighborhood" class="mb-5">
  <div class="d-flex justify-content-between align-items-baseline">
    <h3>Próximos eventos</h3>
    <%= link_to 'Ver todos os eventos', events_path, class: "link-text" %>
  </div>
  <% @elements = @events.take(5) %>
  <%= render "shared/cards/card_event", elements: @elements %>
</section>

<section id="neighborhood-neighborhood" class="mb-5">
  <div class="d-flex justify-content-between align-items-baseline">
    <h3>Locais mais procurados</h3>
    <%= link_to 'Ver todos os locais', locations_path, class: "link-text" %>
  </div>
</section>

  <!-- Implementado em 03-09-23  -->

   <%= render "shared/cards/card_location", elements: @elements %>
